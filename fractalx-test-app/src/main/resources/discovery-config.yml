fractalx:
  discovery:
    enabled: true
    mode: HYBRID
    registry-url: http://localhost:8761

# Service instances configuration
instances:
  order-service:
    - host: localhost
      port: 8081
  payment-service:
    - host: localhost
      port: 8080

# Service dependencies
dependencies:
  order-service:
    - payment-service