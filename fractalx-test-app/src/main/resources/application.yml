spring:
  application:
    name: fractalx-test-app

server:
  port: 8080

fractalx:
  discovery:
    enabled: true
    mode: HYBRID
    host: localhost
    heartbeat-interval: 30000
    instance-ttl: 90000

logging:
  level:
    com.fractalx: DEBUG
    org.springframework.cloud: INFO